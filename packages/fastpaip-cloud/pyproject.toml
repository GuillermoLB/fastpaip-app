[project]
name = "fastpaip-cloud"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "GuillermoLB", email = "guillelb99@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fastpaip-cloud = "fastpaip_cloud:main"

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
